# C언어 if

C언어에서 조건문, if의 구조는

```c
if(조건)
{
    실행문
}
```

이렇게 구성되어있다.    

조건에는

```c
a > b   : a가 b보다 크다
a < b   : a가 b보다 작다
a >= b  : a가 b 이상
a <= b  : a가 b 이하
a == b  : a랑 b랑 같다
a != b  : a는 b와 같지 않다
```

