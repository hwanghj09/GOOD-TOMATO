# 디렉토리 구조
[AD]

리눅스는 루트 디렉토리(`/`)를 기준으로 모든 폴더가 연결됩니다.

## 자주 보는 디렉토리

- `/` : 최상위(루트)
- `/home` : 사용자 홈 디렉토리
- `/etc` : 설정 파일
- `/var` : 로그/가변 데이터
- `/usr` : 프로그램과 라이브러리
- `/tmp` : 임시 파일

## 예시

```bash
/
├─ home
├─ etc
├─ var
├─ usr
└─ tmp
```