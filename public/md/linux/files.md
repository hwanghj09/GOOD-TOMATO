# 파일/디렉터리

리눅스는 모든 것을 파일로 다룹니다. 기본적인 파일/폴더 작업 명령어를 익혀봅시다.

## 자주 쓰는 명령어

- 파일 목록: `ls`
- 파일 복사: `cp`
- 파일 이동/이름 변경: `mv`
- 파일 삭제: `rm`
- 폴더 만들기: `mkdir`

예시:

```bash
mkdir practice
cd practice
cp ../sample.txt .
ls
```
